<div class="digg_pagination">
    <div clas="page_info">
      <%= page_entries_info @trends %>
    </div>
    <%= will_paginate @trends, :container => false %>
  </div>
<br/>
<table class="table table-striped">
  <tr>
  <th>Counter</th>
  <th>Trendword</th>
  <th>Category</th>
  <th>First seen</th>
  <th>Last seen</th>
  </tr>
    <% @trends.each do |trend| %>
    <tr>
    <td> <%= trend.counter %> </td> 
    <td> <%= link_to(trend.trendy_word, trend) %></td>
    <td> <%= trend.category %></td>
    <td><%= trend.created_at %></td>
    <td> <%= trend.updated_at %> </td>
    
</tr>
<% end %>
</table>
<div class="digg_pagination">
    <div clas="page_info">
      <%= page_entries_info @trends %>
    </div>
    <%= will_paginate @trends, :container => false %>

     <span style="float:right">
  <%= link_to "json", trends_path(:format=>:json) %>
  <%= link_to "xml", trends_path(:format=>:xml) %>
  </span></h3>
    </div>
